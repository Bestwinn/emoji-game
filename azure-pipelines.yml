trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: Docker@2
  displayName: Build and push Docker image
  inputs:
    containerRegistry: 'DockerHubConnection'
    repository: 'bestwin/emoji-game'
    command: 'buildAndPush'
    Dockerfile: '**/Dockerfile'
    tags: |
      latest
